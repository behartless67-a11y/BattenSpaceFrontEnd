# Azure Entra ID (formerly Azure AD) Configuration
# Get these values from your Azure Portal > App Registrations

# The Application (client) ID from your Azure App Registration
NEXT_PUBLIC_AZURE_CLIENT_ID=your-client-id-here

# The Directory (tenant) ID from your Azure App Registration
NEXT_PUBLIC_AZURE_TENANT_ID=your-tenant-id-here

# The redirect URI after successful login (should match your Azure app registration)
# For local development: http://localhost:3000
# For production: https://thebattenspace.org
NEXT_PUBLIC_REDIRECT_URI=https://thebattenspace.org

# The redirect URI after logout (should match your Azure app registration)
NEXT_PUBLIC_POST_LOGOUT_REDIRECT_URI=https://thebattenspace.org

# Comma-separated list of Entra group IDs that are allowed to access the application
# Leave empty to allow all authenticated users
# Example: abc123-def456-ghi789,xyz987-uvw654-rst321
NEXT_PUBLIC_AUTHORIZED_GROUPS=
